path to OBJ/MTL folder
OBJ file name
rotation{alpha, beta, r}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
cam{x, y, z, tx, ty, tz}
